# ~github~

mutation add_check_run($repo:ID!,$sha:GitObjectID!,$text:String!) {
    createCheckRun(input:{
        headSha:$sha
        name:$text
        repositoryId:$repo
        status:COMPLETED
        conclusion:NEUTRAL
    }) {
        # TODO: Fill this in?
    }
}
